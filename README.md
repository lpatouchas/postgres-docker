# postgres-docker
A Postgres docker with init script.

Build the Dockerfile and to execute it run the following: <br />
**docker run --name my-postgres -p 5432:5432 local-postgres**

where local-postgres is the name of the previously build image
